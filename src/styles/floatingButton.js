import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  btnStyle: {
    height: 50,
    width: 50,
    borderRadius: 100,
    position: 'absolute',
    bottom: 0,
    right: 0,
  },
});

export default styles;
